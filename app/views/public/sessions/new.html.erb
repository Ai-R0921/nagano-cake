<div class="container">
  <h3>ログイン</h3>
  <div class="row mt-5 mb-3">
    <div class="col-4 text-center">
      <h5>会員の方はこちらからログイン</h5>
    </div>
  </div>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="row mb-3 justify-content-start">
      <%= f.label :email, "メールアドレス", class:"col-3 text-right" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"col-4" %>
    </div>

    <div class="row mb-5">
      <%= f.label :password, "パスワード", class:"col-3 text-right" %>
      <%= f.password_field :password, autocomplete: "current-password", class:"col-4" %>
    </div>

    <div class="row mb-6">
      <div class="col-5 text-right">
        <%= f.submit "ログイン", class:"btn btn-primary" %>
      </div>
    </div>
  <% end %>
  <div class="row mt-5 mb-3">
    <div class="col-4 text-center">
      <h5>会員登録がお済みでない方</h5>
    </div>
  </div>
  <div class="row">
    <div class="col-5 text-right">
      <a href="/customers/sign_up">こちら</a>から新規登録を行ってください。
    </div>
  </div>
</div>

